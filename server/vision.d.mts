import type { Plugin } from 'vite'
/** Local-only vision API: WD14 tagging and object detection over generated images. */
export function switchgenVision(): Plugin
/**
 * Why a reader that needs `peak` bytes must not start with `free` of `total`
 * bytes free, as the sentence the page shows, or null when it may.
 */
export function memoryRefusal(peak: number, free: number, total: number, floorPercent?: number, margin?: number): string | null
